
export interface ABStripeMetadata {

    /**
     * id of the table, which is in turn its name
     */
    business_id: string;

    /**
     * id of the table, which is in turn its name
     */
    order_id: string;

    /**
     * id of the table, which is in turn its name
     */
    payment_id: string;
}